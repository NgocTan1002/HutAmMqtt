{
  "name": "@nhiet-am-mqtt/frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx --no-install vite",
    "build": "npx --no-install tsc -b && npx --no-install vite build",
    "preview": "npx --no-install vite preview",
    "check": "npx --no-install tsc -b --pretty false"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.4.1",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "socket.io-client": "^4.8.1",
    "vite": "^6.3.5"
  },
  "devDependencies": {
    "@types/react": "^19.1.2",
    "@types/react-dom": "^19.1.2",
    "typescript": "^5.8.3"
  }
}
